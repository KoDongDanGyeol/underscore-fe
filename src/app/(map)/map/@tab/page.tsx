interface PageProps extends React.PropsWithChildren {
  //
}

const Page = (props: PageProps) => {
  // const { } = props

  return (
    <div>
      <h2>주변정보(/map/@tab)</h2>
    </div>
  )
}

export default Page
